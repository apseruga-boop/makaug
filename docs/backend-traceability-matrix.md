# MakaUg Backend Traceability Matrix

Date: 2026-05-03

## Task 15 Traceability Addendum

`npm run probe:backend-connections` has been added as the repeatable backend gate probe. It confirms source wiring and live unauthenticated/API health without creating production data. The latest audit evidence is:

- Production `/api/health` returned `200`, `ok: true`, and database `ok: true`.
- Production `/api/health/migrations` confirms `033_task3_engagement_crm.sql` and `034_task4_super_admin_alerts_payments.sql` are applied.
- Production public backend endpoints `/api/ai/model-card`, `/api/advertising/packages`, and `/api/mortgage-rates` are reachable.
- Production `/api/mortgage-rates` currently reports `source: fallback`, so bank/rate copy must remain indicative.
- Anonymous requests to admin APIs and role dashboards are rejected.
- Local env did not include live owner/provider secrets; therefore live super_admin creation/login, provider sends, payment provider execution, Google Places, SMS/WhatsApp delivery, and OpenAI/LLM tool execution are not marked complete.

This matrix uses **Working** only for code paths with source/test/probe evidence. Anything requiring credentials, live owner login, live provider delivery, live scheduled cadence, or intentional production test data remains **Partial**.

This audit maps public and protected product actions to backend endpoints, durable records, provider logs, CRM/lead activity, admin visibility, analytics, and tests. Status values are honest launch-readiness labels:

- Working: backend endpoint/service/model/log path exists and is covered by tests or probes.
- Partial: backend exists but provider credentials, live login, scheduling, or full dashboard proof remains unverified.
- Missing: no durable backend connection was found.

## Backend Connection Matrix

| Feature | Frontend route/component | Button/form/CTA | API endpoint | Backend service | Database/model/log | Notification/WhatsApp/email event | CRM/lead event | Admin visibility | Analytics event | Test coverage | Status | Notes / next action |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Sign in | `/login`, AuthDrawer | Continue/password | `POST /api/auth/login` | `routes/auth.js`, `authFlowService` | `users`, cookie/JWT | admin login audited for admin roles | none | `/admin` audit logs for admin/super_admin | none | `test:go-live-p0` static auth guard tests | Working | Live owner login still needs credentials. |
| Create account | AuthDrawer | Create account | `POST /api/auth/register` | `routes/auth.js`, `authFlowService` | `users`, `otps`, role profile records after verify | `otp_sent`, later account-created notification | role profile/preference records after OTP | users/admin summary | none | `test:go-live-p0` now checks email and phone required | Working | Backend now requires email and phone/WhatsApp. |
| Student signup | AuthDrawer, `/student-signup` | Student role card | `POST /api/auth/register`, `POST /api/auth/verify-otp` | `authFlowService.ensurePostVerificationRecords` | `users`, `student_preferences` | `account_created_student`, `otp_sent`, `otp_verified` | student profile/preference | admin users | none | `test:go-live-p0` role redirect | Working | Live OTP provider depends on credentials or safe fallback. |
| Broker signup | AuthDrawer, `/broker-signup` | Broker role card | `POST /api/auth/register` | `authFlowService` | `users.profile_data.broker_review_status` | `new_broker_verification_request` | signup notification | admin users/brokers | none | `test:go-live-p0` role redirect | Working | Verification workflow exists; live review still owner-operated. |
| Field agent signup | AuthDrawer, `/field-agent-signup` | Field Agent role card | `POST /api/auth/register` | `authFlowService` | `users.profile_data.field_agent_application_status` | `new_field_agent_application` | signup notification | admin field agents/users | none | `test:go-live-p0` role redirect | Working | Contract/payment depth remains partial. |
| Advertiser signup | AuthDrawer, `/advertiser-signup` | Advertiser role card | `POST /api/auth/register`, `POST /api/advertising/inquiries` | `authFlowService`, `advertisingCatalogService` | `users`, `advertising_inquiries` | `advertiser_signup_received` | advertiser lead | `/admin/advertising` | advertising inquiry | `test:go-live-p0` | Working | Paid campaign provider credentials remain required. |
| OTP send | AuthDrawer | Send verification code | `POST /api/auth/request-otp`, register flow | `smsService`, `emailService` | `otps`, `notifications` | `otp_sent` | none | `/admin/notifications` | none | `test:go-live-p0` | Partial | Production OTP requires SMS/email provider or admin override; OTP values are not logged. |
| OTP verify | AuthDrawer | Verify code | `POST /api/auth/verify-otp` | `authFlowService`, `adminSecurityService` | `users.phone_verified`, role profile tables | `otp_verified`, account-created email/log | profile/preference records | admin users/audit for admin roles | none | `test:go-live-p0` | Working | Redirect uses role-aware dashboard mapping. |
| Password reset | AuthDrawer | Forgot password | `POST /api/auth/request-password-reset`, `POST /api/auth/reset-password` | `smsService`, `emailService` | `otps`, `users.password_hash` | `otp_sent`/password reset provider log | none | notifications | none | static route coverage | Partial | Email/SMS provider-dependent; no dedicated EmailLog entry found for reset. |
| Change password | Account/settings dashboard | Change password | `POST /api/auth/change-password` | `adminSecurityService` | `users.password_hash`, `admin_security_settings` for admins | none | none | audit for admin/super_admin | none | `test:go-live-p0` static audit check | Working | Non-admin password changes not separately logged. |
| Contact preferences | dashboards/account | Save preferences | `PATCH /api/auth/me`, `/api/property-seeker/contact-preferences` | auth/property-seeker routes | `users`, `property_seeker_profiles` | none | activity for property seeker route | account/admin users | none | static API coverage | Working | Role-specific settings UI exists; deeper E2E proof remains needed. |
| Language preference | AuthDrawer/dashboards | Save language | `PATCH /api/auth/me`, `/api/property-seeker/language` | auth/property-seeker routes | `users`, `property_seeker_profiles` | none | property seeker activity | account/admin users | language UI events | probes/static tests | Working | Non-English copy remains partial and tracked in i18n audit. |
| Property finder dashboard load | `/dashboard` | dashboard route | `GET /api/property-seeker/dashboard` | `routes/property-seeker.js` | profiles, preferences, saved listings/searches, recent, notes, comparisons, viewings, callbacks | none | activity rows available | super_admin preview | dashboard UI events | `test:go-live-p0` | Partial | Backend data endpoint exists; authenticated live user proof still needed. |
| Saved listings | `/dashboard`, cards | Save/remove | `GET/POST/DELETE /api/property-seeker/saved-listings` | property-seeker route | `saved_listings` | none | `property_seeker_activities` | demand/admin via dashboard | save listing event | static API coverage | Working | Live user E2E still needed. |
| Saved searches | category pages/dashboard | Save Search | `GET/POST/PATCH/DELETE /api/property-seeker/saved-searches` | property-seeker route, `alertSchedulerService` | `saved_searches.location_object` | `saved_search_created` notification | `saved_search` lead/activity | `/admin/alerts`, launch control | save search event | `test:go-live-p0` matcher | Working | Scheduler/live cron status still needs operational confirmation. |
| Alerts | saved searches/admin | Create Alert/retry | listing activation calls `matchListingToSavedSearches`; admin retry | `alertSchedulerService`, `notificationRetryService` | `alert_matches`, `notifications` | `alert_match_found` | demand signal | `/admin/alerts` | alert analytics | `test:go-live-p0` matcher/admin routes | Partial | Matching service exists; production scheduler cadence not verified. |
| Recommendations | `/dashboard` | view recommendations | `GET /api/property-seeker/recommendations` | property-seeker route | approved `properties` | none | activity possible | dashboard preview | recommendation views | static API coverage | Partial | Safe empty states exist; relevance/ranking still basic. |
| Recently viewed | property cards/dashboard | View property | `GET/POST /api/property-seeker/recently-viewed` | property-seeker route | `recently_viewed_listings` | none | activity | dashboard | recently viewed event | static API coverage | Working | Needs browser proof with authenticated user. |
| Enquiries/WhatsApp contacts | property cards/detail | WhatsApp/enquiry | `POST /api/properties/:id/whatsapp-click`, `POST /api/properties/:id/inquiries` | properties route, `leadService` | `property_inquiries`, `leads`, `lead_activities`, `notifications` | `whatsapp_contact_initiated`, `enquiry_sent` | created as CRM lead/activity | `/admin/leads`, `/admin/notifications` | click event | `test:go-live-p0` now checks CRM activity wiring | Working | WhatsApp provider send still provider-dependent; click logging is durable. |
| Viewings | listing/dashboard | Book Viewing | `POST /api/property-seeker/viewings` | property-seeker route, `leadService` | `viewing_bookings`, `leads`, `lead_activities`, `notifications` | `viewing_requested` | viewing lead/activity | `/admin/leads`, launch control | viewing event | `test:go-live-p0` static/API | Working | Broker dashboard live proof remains partial. |
| Callbacks | listing/dashboard | Request Callback | `POST /api/property-seeker/callbacks` | property-seeker route, `leadService` | `callback_requests`, `leads`, `lead_activities`, `notifications` | `callback_requested` | callback lead/activity | `/admin/leads`, launch control | callback event | `test:go-live-p0` static/API | Working | Broker dashboard live proof remains partial. |
| Student preferences | `/student-dashboard` | Save preference | `GET/PATCH /api/student/preferences` | student route | `student_preferences` | none | activity endpoint | admin/users | student activity | static API coverage | Working | Authenticated live proof still needed. |
| Student saved searches | student dashboard/category | Save student search | `GET/POST /api/student/saved-searches` | student route | `saved_searches` | `saved_search_created` via saved-search flow | demand | `/admin/alerts` | save search event | static API coverage | Working | Alert matching shared with saved searches. |
| Broker dashboard | `/broker-dashboard` | listings/leads/viewings | existing protected route/UI; admin APIs | admin/property APIs | `properties`, `leads`, `viewing_bookings`, `callback_requests` | notifications | lead records | `/admin/leads`, broker shell | dashboard events | protected route tests | Partial | Backend endpoints exist but broker-specific API aggregation is partial. |
| Field agent dashboard | `/field-agent-dashboard` | tasks/submitted listings | protected shell/admin APIs | admin/users/properties | `users.profile_data`, properties | notifications | leads/tasks partial | `/admin/field-agents` | dashboard events | protected route tests | Partial | Dedicated field-agent task backend remains partial. |
| Advertiser dashboard | `/advertiser-dashboard` | campaign/payment | `GET /api/advertising/dashboard`, `POST /api/advertising/campaigns`, payment link/status | `advertisingCatalogService`, `paymentProviderService`, `leadService` | `advertising_inquiries`, `advertising_campaigns`, `invoices`, `payment_links`, `leads` | `campaign_submitted`, `payment_link_created` partial | advertiser lead | `/admin/advertising`, `/admin/revenue` | campaign events | `test:go-live-p0` | Partial | Records and manual fallback exist; real provider/webhook live credentials unverified. |
| Admin launch control | `/admin/launch-control` | owner dashboard | admin API summary/log endpoints | admin routes, alert/payment/retry services | leads/listings/invoices/logs/audit | provider status/logs | all leads | `/admin/launch-control` | launch health | `test:go-live-p0` static | Partial | Business-control UI exists; live super_admin login not verified. |
| Admin docs | `/admin/docs` | View docs | protected page/static docs | public sanitizer route guard | docs files | none | none | admin docs route | none | `test:go-live-p0` | Working | Public sanitizer removes docs from anonymous HTML. |
| CRM/leads | `/admin/crm`, `/admin/leads` | assign/update/note/task | `GET/PATCH /api/admin/leads`, activities/tasks | `leadService`, admin routes | `contacts`, `leads`, `lead_activities`, `lead_tasks`, `lead_assignments` | notifications by source | central CRM | admin CRM/leads | lead events | `test:go-live-p0` | Working | Live operational data depends on production traffic. |
| Listing online | `/list-property` | Submit listing | `POST /api/properties`, OTP endpoints | `listingReferenceService`, `listingModerationService`, `leadService`, email/log services | `properties`, `property_images`, `property_moderation_events`, `email_logs`, `whatsapp_message_logs`, `notifications`, `leads` | `listing_submitted`, `new_listing_pending_review` | listing owner lead/activity | admin moderation/notifications | AI learning event | `test:go-live-p0` static; route probes | Working | Safe end-to-end DB proof needs test/preview submission, not live fake data. |
| Listing via WhatsApp AI | `/list-property` | List via WhatsApp AI | `POST /api/properties/listing-intent` plus `wa.me` handoff | `leadService`, `notificationLogService`, `whatsappMessageLogService`, `aiLearningCaptureService` | `leads`, `lead_activities`, `notifications`, `whatsapp_message_logs`, AI learning log when configured | WhatsApp provider handoff logged as `manual_url` / provider-missing until inbox credentials are configured | Creates listing-owner lead/activity before opening WhatsApp | Admin CRM / notifications / WhatsApp logs | `list_property_path_selected`, `list_property_whatsapp_clicked` | go-live tests + click probe | Working with provider fallback | Browser opens WhatsApp externally; delivery confirmation depends on live WhatsApp provider/inbox. |
| Address lookup/map pin | `/list-property` | Find address/place/Confirm pin | frontend provider, no server geocode endpoint | Google Places public key/fallback | location fields in `properties.extra_fields` and columns | provider-missing UI/log | listing lead carries location | admin listing review | location event | route/browser probes | Partial | Google Places credentials/live browser selection still not verified. |
| Listing photos | `/list-property` | Upload photos | `POST /api/properties` accepts image URLs/data URLs | properties route | `property_images` | listing submitted logs include image count | listing lead metadata | admin moderation | upload events | static checks | Partial | Production file storage/upload service not deeply audited. |
| Listing OTP | `/list-property` | Send/verify OTP | `POST /api/properties/request-submit-otp`, `/verify-submit-otp` | `smsService`, `emailService`, listing token util | `otps`, submit token | OTP notification/log | none | notifications | OTP events | static checks | Partial | Provider credentials required for live sends. |
| Search sale/rent/land/commercial/student | category routes | Search/filter | `GET /api/properties` | properties route | `properties` | none | no-results can create need request | public/admin listings | search analytics | public route probes | Working | Empty states are useful; live inventory varies. |
| Brokers | `/brokers` | broker card/profile | public page/admin agent APIs | admin/agents route | `agents`, `users` | none | broker lead partial | admin agents | click event | click probe | Partial | Public broker profile route depth still limited. |
| No-results/Tell MakaUg | category/help | Tell MakaUg what you need | `POST /api/contact/looking-for-property` | contact route, `leadService` | `property_requests`, `leads`, `lead_activities`, `notifications` | property need notification | property need lead/activity | `/admin/leads`, property requests | demand event | `test:go-live-p0` now checks CRM/log wiring | Working | Confirmation email not yet implemented for this specific route. |
| AI chatbot page | `/discover-ai-chatbot` | Ask AI | `POST /api/ai/assistant-reply` | `aiService`, `aiLearningCaptureService`, `leadService` | `ai_events`, leads for handoff/fraud/mortgage/advertiser/listing intents, notifications | `human_handoff_required` and intent logs | lead for actionable intents | `/admin/whatsapp-inbox`, AI events/admin logs | AI learning event | `test:go-live-p0` now checks backend logging | Partial | Works with template fallback; full LLM/tool execution depends on OpenAI/provider config. |
| AI search intents | AI chatbot | Search prompt | `POST /api/ai/assistant-reply` | `aiService` | AI event | none | demand signal only | AI/admin logs | AI learning | static tests | Partial | Does not yet execute listing search tool server-side. |
| AI save/search/viewing/callback tools | AI chatbot | task prompt | frontend routes/API handoff | partial | partial | partial | lead/handoff for actionable intents | admin logs | AI learning | static tests | Partial | Need full tool execution before calling AI complete. |
| Mortgage calculator | `/mortgage` | Calculate | client calculator + `mortgageCalculatorService` tests | mortgage calculator service | none | none | none | none | calculator event | formula tests | Working | Calculator formula tested. |
| Mortgage help form | `/mortgage` | Request mortgage help | `POST /api/mortgage/enquiry` | mortgage route, `leadService`, email/log services | `mortgage_enquiries`, `leads`, `email_logs`, `notifications` | `mortgage_lead_received` | mortgage lead/activity | admin leads/launch control | mortgage event | `test:go-live-p0` now checks backend wiring | Working | Provider email may log mocked/failure status. |
| Mortgage rates/bank comparison | `/mortgage` | Read providers | `GET /api/mortgage`, `PUT /api/mortgage` admin | mortgage route | `mortgage_providers` or fallback config | none | none | admin update | none | route tests | Partial | Fallback rates are indicative; no live daily rates API verified. |
| Help request | `/help` | Support form | `POST /api/contact/help-request` | contact route, `leadService`, email/log services | `leads`, `email_logs`, `notifications` | `help_request_submitted`, admin alert | support lead/activity | `/admin/leads`, emails/notifications | help event | `test:go-live-p0` | Working | Provider delivery depends on config; safe logs exist. |
| Careers form | `/careers` | career interest | `POST /api/contact/career-interest` | contact route, `leadService`, email/log services | `leads`, `email_logs`, `notifications`; no dedicated CareerSubmission table | `career_interest_submitted`, `new_career_interest` | career lead/activity | `/admin/leads`, `/admin/emails`, `/admin/notifications` | career interest event | `test:go-live-p0` static/backend wiring | Working | Launch-ready lead capture exists with provider fallback logging; owner can add a dedicated careers table/CV upload later if hiring volume needs it. |
| Fraud report | `/report-fraud`, `/anti-fraud` | Report listing | `POST /api/contact/report-listing` | contact route, `leadService`, email/log services | `report_listings`, `leads`, `email_logs`, `notifications` | `fraud_report_received` | fraud lead/activity | admin reports/leads/notifications | fraud event | `test:go-live-p0` now checks log wiring | Working | Dedicated `FraudReport` table name is not used; current table is `report_listings`. |
| How-to video slots | public pages | Open video modal | config-only | `config/howToVideos.js` | no DB model | none | none | admin docs/content status | video UI events | `test:go-live-p0`, click probe | Partial | YouTube IDs are intentionally placeholders until owner provides URLs. |
| Email provider | all events | send email | service calls MS Graph/SMTP/Resend/webhook | `emailService`, `emailLogService` | `email_logs`, `notifications` | safe mocked/provider-missing status | related lead/listing IDs | `/admin/emails`, `/admin/notifications` | email event | `test:go-live-p0` | Partial | Live provider credentials not proven in this audit. |
| WhatsApp provider | listing/chat/notifications | WhatsApp send/link | Meta/Twilio service, wa.me links | `whatsappNotificationService`, `whatsappMessageLogService` | `whatsapp_message_logs`, WhatsApp inbox tables | provider-missing/mock state | WhatsApp lead/activity | `/admin/whatsapp-inbox`, logs | WhatsApp click event | click probe/static tests | Partial | Live Meta/Twilio credentials not proven. |
| SMS provider | OTP/alerts | SMS OTP/alerts | `smsService` | Twilio/Africa's Talking | `notifications` | provider-missing/mock state | none | `/admin/notifications` | OTP event | static tests | Partial | Live SMS credentials not proven. |
| Payments | advertiser/admin | payment link/webhook/manual paid | `/api/advertising/...`, `/api/admin/payments/invoices/:id/manual-paid` | `paymentProviderService` | `invoices`, `payment_links`, `admin_audit_logs` | `payment_link_created` partial | advertiser lead/campaign | `/admin/revenue`, `/admin/advertising` | payment event | `test:go-live-p0` | Partial | Manual fallback works; real payment provider signature verification is generic and provider-dependent. |
| Protected admin APIs | `/api/admin/*` | all admin actions | `router.use(requireAdminApiKey)` | `middleware/auth.js` | role/JWT/API key guard | audit for sensitive actions | admin actions | admin modules | admin events | `test:go-live-p0` protected path checks | Working | `ADMIN_API_KEY` access remains supported; owner JWT role also supported. |
| Public route security | public routes | anonymous browsing | static fallback sanitizer | `publicHtmlSanitizer` | none | none | none | none | route probes | public route probes/tests | Working | Latest live route probes passed before this backend audit. |

## High-Risk Findings

1. Live super_admin login was not performed in this audit because credentials were not available in the local environment.
2. Email, WhatsApp, SMS, Google Maps, OpenAI/LLM, and payment provider credentials still need production verification; fallback logging exists for the audited paths.
3. AI chatbot backend now logs conversation events and creates CRM leads for actionable handoff/fraud/mortgage/advertiser/listing intents, but it does not yet execute full listing search/save/viewing/callback tools server-side.
4. Saved-search alert matching exists and prevents duplicates via database conflict handling, but scheduler/cron cadence has not been live-verified.
5. Mortgage rates are database-backed or fallback-configured and must be treated as indicative unless a verified rates source is configured.
6. Careers now has a dedicated backend lead-capture endpoint and email/notification logs, but no dedicated CareerSubmission table or file-upload storage.
7. Full non-English translation coverage and YouTube video IDs remain partial and owner-provided.

## Owner Go-Live Backend Checklist

1. Confirm migrations `033_task3_engagement_crm.sql` and `034_task4_super_admin_alerts_payments.sql` are applied using `/api/health/migrations`.
2. Provide `SUPER_ADMIN_EMAIL`, `SUPER_ADMIN_INITIAL_PASSWORD`, `SUPER_ADMIN_PHONE` optional, `DATABASE_URL`, and `JWT_SECRET`, then run `npm run admin:create-super`.
3. Sign in as super_admin, change the initial password, and confirm `/admin/launch-control`, `/admin/docs`, `/admin/crm`, `/admin/leads`, `/admin/advertising`, `/admin/revenue`, `/admin/notifications`, `/admin/emails`, and `/admin/whatsapp-inbox`.
4. Submit one test listing only in test/preview mode or with an agreed production QA flag; verify reference, `email_logs`, `notifications`, `whatsapp_message_logs`, CRM lead, and admin moderation.
5. Verify provider credentials for email, WhatsApp, SMS, payment, Google Places, and OpenAI/LLM.

## Task 16 Traceability Addendum

The protected owner proof route `/admin/setup-status` now maps live operational proof actions to backend records/logs/admin visibility.

| Feature | Frontend route/component | CTA/form | API endpoint | Backend service | Database/model/log | Notification/email/WhatsApp event | CRM/lead event | Admin visibility | Test coverage | Status | Notes / next action |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Owner setup status | `/admin/setup-status` | Refresh Status | `GET /api/admin/setup-status` | admin routes, health queries, provider env checks | `users`, `admin_security_settings`, `schema_migrations`, `audit_logs`, counts from leads/listings/logs | none | none | setup status page, launch control link | `test:go-live-p0`, backend probe anonymous block | Working in source | Owner must log in live to view. |
| Safe property submission proof | `/admin/setup-status` | Run safe property submission test | `POST /api/admin/setup-status/property-submission-test` | `listingReferenceService`, `leadService`, log services | `properties`, `leads`, `lead_activities`, `email_logs`, `notifications`, `whatsapp_message_logs`, `audit_logs` | `listing_submitted` | listing-submission CRM lead/activity | setup status, moderation/leads/logs | `test:go-live-p0` source checks | Working in source | Owner must run live; record is test-labelled and not approved. |
| Provider proof | `/admin/setup-status` | Test email/WhatsApp/SMS/Google/OpenAI/payment | `POST /api/admin/setup-status/provider-test` | email/WhatsApp/notification log services, provider env checks | `email_logs`, `whatsapp_message_logs`, `notifications`, `audit_logs` | provider-specific test log | none | setup status, email/notification/WhatsApp logs | `test:go-live-p0` source checks | Working in source | Provider sends still depend on configured credentials. |
| AI smoke proof | `/admin/setup-status` | Run AI chatbot smoke test | `POST /api/admin/setup-status/ai-smoke-test` | `llmProvider`, `leadService`, notification logs | `leads`, `lead_activities`, `notifications`, `audit_logs` | provider-missing/logged AI prompt events | actionable AI intents create leads | setup status, leads/logs | `test:go-live-p0` source checks | Partial | Full LLM/tool execution still needs provider credentials and live run. |
| Alert matcher proof | `/admin/setup-status` | Run alert matching now | `POST /api/admin/setup-status/run-alert-matcher` | `alertSchedulerService.matchListingToSavedSearches` | `saved_searches`, `alert_matches`, `notifications`, `audit_logs` | `alert_match_found` | demand signal via saved search | setup status, `/admin/alerts` | matcher tests + source checks | Working in source | Cron cadence still owner action. |
| Viewing/callback proof | `/admin/setup-status` | Create viewing/callback test | `POST /api/admin/setup-status/viewing-callback-test` | `leadService`, notification logs | `viewing_configs`, `viewing_bookings`, `callback_requests`, `leads`, `notifications`, `audit_logs` | viewing/callback launch test log | viewing/callback CRM leads | setup status, leads/viewings/callbacks | source checks | Working in source | Owner must run live. |
| Advertising/payment proof | `/admin/setup-status` | Create payment fallback test | `POST /api/admin/setup-status/advertising-payment-test` | `paymentProviderService.markInvoicePaidManually` | `advertising_campaigns`, `invoices`, `payment_links`, `admin_audit_logs`, `audit_logs` | payment fallback/audit | advertiser campaign record | setup status, advertising/revenue | source checks | Working in source | Real provider remains unverified until credentials/webhook test. |
| Mortgage/help/careers/fraud proof | `/admin/setup-status` | Test mortgage/help/careers/fraud | `POST /api/admin/setup-status/support-flow-test` | `leadService`, email/notification log services | `mortgage_enquiries`, `report_listings`, `leads`, `email_logs`, `notifications`, `audit_logs` | mortgage/help/careers/fraud log events | CRM leads for each flow | setup status, leads/reports/logs | source checks | Working in source | Owner must run live. |
